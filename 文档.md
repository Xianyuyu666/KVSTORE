# kvstore M2 设计文档：并发层（线程池）

> 前置：M1 封板（Reactor + 帧协议 + Conn + 日志 + 四连测全绿）
> 时间：约 3-4 天（M2a 组件 2 天 + M2b 接入 1-2 天）
> 核心目标：**让"业务处理"不再阻塞事件循环**

---

## 1. M2 要解决什么问题

当前服务器是**单线程事件循环**：一个线程做所有事——accept、读、切帧、echo、写。

问题在 M3 会爆发：真正的 KV 操作（解析命令、序列化大 value、访问存储）是**耗时任务**。如果它们全在事件循环里做：

```
一个客户端请求解析 2ms → 事件循环被占 2ms → 其他 1000 个连接全部排队等
```

**单线程事件循环 = 一个慢请求卡死所有连接。**

M2 的解法：**线程池**——事件循环（IO 线程）只负责"收发数据"，把耗时的业务处理丢给工作线程，自己立刻回去循环。

## 2. 架构总览：单 Reactor + 线程池（半同步半异步）

```
                    ┌─────────────────────────────┐
   客户端连接 ──►    │  IO 线程（主线程，现有 Reactor） │
                    │  epoll 事件循环               │
                    │  ① accept                    │
                    │  ② 读 → 攒帧                  │
                    │  ③ 帧完整 → 【提交任务】────┐   │
                    │  ④ 写状态机（EPOLLOUT）     │   │
                    └─────────────────────────────┘   │
                                                      ▼
                                          ┌─────────────────────┐
                                          │  线程池（M2 要写的）     │
                                          │  任务队列              │
                                          │  工作线程 1  工作线程 2  │
                                          │  工作线程 3  工作线程 4  │
                                          └─────────────────────┘
                                                      │
                                          （业务处理完，结果送回 IO 线程，
                                           M2b 再做这步，M2a 不做）
```

**分工**：
- IO 线程：管网络（收发、状态机）——响应快
- 工作线程：管业务（解析、存储、计算）——并发干活

## 3. M2a：线程池组件（本周主任务）

独立于网络层，先写纯组件 + 测试验证。

### 3.1 接口设计

```cpp
// include/kvstore/concurrent/thread_pool.h
#pragma once
#include <functional>
#include <mutex>
#include <condition_variable>
#include <queue>
#include <thread>
#include <vector>

class ThreadPool {
public:
    explicit ThreadPool(size_t n_threads);   // 创建 n 个工作线程
    ~ThreadPool();                            // 析构：停止并回收所有线程

    void submit(std::function<void()> task);  // 提交一个任务

private:
    void worker_loop();                       // 每个工作线程的循环

    std::vector<std::thread> workers;         // 工作线程数组
    std::queue<std::function<void()>> tasks;  // 任务队列
    std::mutex mtx;                           // 保护 tasks
    std::condition_variable cv;               // 唤醒睡眠的工作线程
    bool stop = false;                        // 停止标志
};
```

### 3.2 关键机制（每个都是必懂点）

**① 任务队列 + mutex**
- 队列被多个工作线程同时访问 → 必须用锁保护
- 规则：**访问 tasks 的任何操作（push/pop/检查空）都要在锁内**

**② 条件变量 cv + unique_lock**

工作线程没任务时不能空转（烧 CPU），要**睡觉**等通知：

```cpp
std::unique_lock<std::mutex> lock(mtx);
cv.wait(lock, [this]{ return stop || !tasks.empty(); });
// wait 内部：① 检查条件 ② 不满足就释放锁并睡眠 ③ 被唤醒后重新拿锁再检查
```

**③ 为什么 wait 必须配锁（核心面试点）**
- "检查队列空"和"去睡觉"必须是**一个原子动作**
- 否则：工作线程检查到队列空 → 还没睡 → submit 推入任务 + notify → 工作线程才睡 → **通知丢失，任务永远没人做**（lost wakeup）
- 条件变量 + 锁就是防这个：检查在锁内，睡眠时释放锁，唤醒后拿回锁

**④ 为什么 wait 要循环条件（spurious wakeup）**
- 被唤醒不代表条件一定满足（可能有多个线程抢同一个任务）
- 所以用 `cv.wait(lock, 条件)` 这种**带条件重载**——唤醒后自动重新检查

**⑤ submit 的实现骨架**

```cpp
void ThreadPool::submit(std::function<void()> task) {
    {
        std::lock_guard<std::mutex> lock(mtx);
        tasks.push(std::move(task));
    }          // 锁内 push，锁外 notify
    cv.notify_one();   // 唤醒一个睡眠的工作线程
}
```

**⑥ 析构（shutdown，最容易死锁的地方）**

```cpp
ThreadPool::~ThreadPool() {
    {
        std::lock_guard<std::mutex> lock(mtx);
        stop = true;              // ① 置停止标志（锁内）
    }
    cv.notify_all();              // ② 唤醒所有线程
    for (auto& t : workers) {
        t.join();                 // ③ 等所有线程退出
    }
}
```

- 置 stop 和 notify_all 的顺序、join 缺一不可
- join 不调 = 程序退出时线程还在跑（崩溃/泄漏）

**⑦ worker_loop 骨架**

```cpp
void ThreadPool::worker_loop() {
    while (true) {
        std::function<void()> task;
        {
            std::unique_lock<std::mutex> lock(mtx);
            cv.wait(lock, [this]{ return stop || !tasks.empty(); });
            if (stop && tasks.empty()) return;   // 停止且无任务 → 退出
            task = std::move(tasks.front());
            tasks.pop();
        }
        task();   // 锁外执行任务（重要！别在锁里干活）
    }
}
```

**关键原则：锁内只做队列操作，任务执行必须在锁外**——否则一个长任务会阻塞所有工作线程抢锁。

### 3.3 测试程序（你自己写）

`src/thread_pool_test.cpp`（或直接改 main）验证：

1. 提交 100 个任务，每个任务打印"线程 id + 任务号"（比如 `[线程id] 执行任务 42`）
2. 验证点：
   - 100 个任务**全部被执行**（数一数输出条数）
   - **多个线程 id 出现**（证明并发执行，不是单线程排队）
   - 程序**正常退出**（析构无死锁、无卡住）
   - 0 警告 + ASan 无泄漏

参考：

```cpp
// main 里
ThreadPool pool(4);
for (int i = 0; i < 100; i++) {
    pool.submit([i]{
        std::cout << "线程 " << std::this_thread::get_id()
                  << " 执行任务 " << i << std::endl;
    });
}
// pool 析构时自动等所有任务完成
```

### 3.4 M2a 验收标准

- [ ] 全量重编译 0 警告，ASan 无泄漏
- [ ] 100 个任务全部执行（输出 100 条）
- [ ] 出现 ≥2 个不同线程 id（并发生效）
- [ ] 程序正常退出（无死锁）
- [ ] 提交 100 个任务但线程池析构时任务没做完 → 行为合理（要么等完，要么定义清楚）
- [ ] 能口头讲清：条件变量为什么配锁？wait 为什么循环？锁内为什么不能执行任务？

## 4. M2b 预告（接入网络层，M2a 完成后做）

- IO 线程把"完整帧"提交为任务 → 工作线程解析 → 生成响应
- 难点：**回写必须回到 IO 线程**（写状态机在 IO 线程）→ 需要跨线程传数据
- 到时候给具体方案，先别想

## 5. 并发基础概念（M2a 过程中会踩）

| 概念 | 一句话 |
|---|---|
| 数据竞争 | 多线程同时读写同一变量，行为未定义 |
| 临界区 | 需要互斥访问的代码段（锁保护的区域） |
| 死锁 | 线程互相等对方释放锁，永远卡住 |
| 条件变量 | "等某个条件"的同步原语，配合锁使用 |
| 原子操作 | 不可分割的操作，无锁编程基础（M2 后期） |

## 6. 面试 10 问（M2a 完事后要能答）

1. 线程池是什么？为什么服务器要用？
2. 任务队列为什么需要锁？
3. 条件变量和互斥锁什么关系？为什么 wait 必须配锁？
4. 什么是 lost wakeup？
5. 什么是 spurious wakeup？wait 为什么要循环？
6. 析构时为什么先置 stop 再 notify_all 再 join？
7. 为什么任务执行要在锁外？
8. 线程池线程数怎么定？（CPU 核数 / IO 密集型 vs CPU 密集型）
9. 单 Reactor + 线程池和"每连接一线程"比有什么优势？
10. 如果任务执行中抛异常会怎样？（要不要 catch）
